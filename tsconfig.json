{
  "compilerOptions": {
    "target": "es5", // ES5 버전으로 컴파일
    "lib": ["dom", "dom.iterable", "esnext"], // 사용할 라이브러리 설정
    "allowJs": true, // JavaScript 파일을 허용
    "skipLibCheck": true, // 라이브러리 타입 체크 생략
    "strict": true, // 엄격한 타입 검사 옵션
    "forceConsistentCasingInFileNames": true, // 파일명 대소문자 일관성
    "esModuleInterop": true, // ES 모듈 인터롭 활성화
    "module": "esnext", // 모듈 시스템을 esnext로 설정
    "moduleResolution": "node", // Node.js 스타일로 모듈 해석
    "resolveJsonModule": true, // JSON 모듈을 허용
    "isolatedModules": true, // 각 파일을 독립된 모듈로 처리
    "jsx": "preserve", // JSX 처리 방식
    "noEmit": true, // 컴파일된 파일 출력 안 함 (Next.js에서 자체적으로 처리)
    "incremental": true, // 빌드 최적화를 위해 incremental 사용
    "strictPropertyInitialization": false, // 프로퍼티 초기화 엄격 모드 비활성화
    "experimentalDecorators": true, // 데코레이터 사용 허용
    "emitDecoratorMetadata": true, // 메타데이터 정보 생성 (TypeORM 등에서 필요)
    "baseUrl": "./src", // 경로 별칭을 사용할 기본 경로
    "paths": { // 경로 별칭 설정
      "@/*": ["*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ], // 포함할 파일
  "exclude": ["node_modules"] // 제외할 파일
}
